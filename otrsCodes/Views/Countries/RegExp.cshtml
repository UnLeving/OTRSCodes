@using (Html.BeginForm("RegExp", "Countries", FormMethod.Post, new { id = "form_RegExp" }))
{
    <h4>RegExp generator</h4>
    <hr />
    <div class="form-group">
        <div class="form-group"> @{Html.RenderAction("CountryDropDown", "Countries");} </div>
        <div id="NetworkDD" class="form-group">  </div>

        <label for="RegExp">RegExp:</label>
        <input type="text" class="form-control" id="RegExp" readonly="readonly">
    </div>
}